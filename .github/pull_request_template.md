## 目标 / 非目标

- 目标：
- 非目标：

## 基线与最高风险

- exact base（commit SHA）：
- exact head（commit SHA）：
- 最高风险（`F0` / `F1` / `F2` / `F3`）：

## 实际变更面

<!-- 列出改动的 owner、用户可见合同或副作用；没有变更请明确写“无”。 -->

## 实际执行的检查

<!-- 只填写实际执行的检查与其结果；可用结果：PASS / FAIL / ENV-BLOCKED / NEEDS-REAL-MACHINE / NOT-RUN。 -->

| 层 | 实际执行的检查 / 结果 |
| --- | --- |
| source / unit | |
| artifact | |
| installed / runtime | |
| live Provider / Science / SSH | |
| signing / release | |

`SOURCE-GREEN`：<!-- 仅完整 exact-candidate source gate 通过时填写 PASS；focused PASS 不能替代。否则填 NOT-RUN。 -->

请勿粘贴 API key、OAuth token、邮箱、私有 URL、一次性 nonce、路径 secret、用户数据或整份日志。
